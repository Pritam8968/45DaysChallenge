class Solution {
public:
    int findDuplicate(vector<int>& nums) {
        map<int, bool>hashMap;
        int ans=0;
        for (int i=0;i<nums.size();i++){
            hashMap[nums[i]]=false;
        }
        for (int i=0;i<nums.size();i++){
            if(hashMap[nums[i]]){
                ans= nums[i];
                break;
            }
            hashMap[nums[i]]=true;
        }
        return ans;
    }
};
